- name: Sample
  hosts: all
  vars:
    message: "The first message"
  tasks:
    - name: Message
      debug:
        msg: "{{ message }}"
    
    - name: Audit
      shell: tail -2 /etc/audit/auditd.rules
